#pragma once
#include <juce_gui_basics/juce_gui_basics.h>

namespace zazzGUI::Colors
{
    // Inline constants usable in header-only modules
    inline const juce::Colour darkColor      = juce::Colour::fromRGB(40, 42, 46);
    inline const juce::Colour lightColor     = juce::Colour::fromRGB(68, 68, 68);
    inline const juce::Colour highlightColor = juce::Colour::fromRGB(255, 255, 190);
}